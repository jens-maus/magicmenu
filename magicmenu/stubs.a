**
**	$Id$
**
**	:ts=8
**

	section text,code

*--------------------------------------------------------------------------

PATCH	macro

	xref	Old\1
	xdef	Call\1

Call\1:
	movem.l a0-a5/d1-d7,-(sp)
	move.l	Old\1(a4),a4
	jsr	(a4)
	movem.l (sp)+,a0-a5/d1-d7
	rts

	endm

*--------------------------------------------------------------------------

	PATCH	OpenWindow
	PATCH	OpenWindowTagList
	PATCH	ClearMenuStrip
	PATCH	SetMenuStrip
	PATCH	ResetMenuStrip
	PATCH	CloseWindow
	PATCH	ActivateWindow
	PATCH	WindowToFront
	PATCH	ModifyIDCMP
	PATCH	ObtainGIRPort
	PATCH	CreateUpfrontHookLayer

*--------------------------------------------------------------------------

	end
